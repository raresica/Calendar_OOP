{% extends 'templates/app.twig' %}

{% block content %}
    <div>

    <div class="calendar-content">
        <div class="calendar-header">
            <button id="prev"><</button>
            <h2><span id="months"> </span> <span id="years"> </span></h2>
            <button id="next">></button>
        </div>

        <div id="calendar-content" >

        </div>

    </div>

    <div class="modal-container"
         id="modal_container">
        <div class="modal">
            <h1 id="modalTitle"></h1>



            <div>
        <h2 >Appointments</h2>


            </div>

<form action="{{route('reservation')}}" method="get" >
{#    <input type="hidden" name="{{ constant('SESSION_KEY', csrf) }}" value="{{ csrf.token }}">#}

    <div>
                <input id="selected_date" type="hidden" name="date">

                <button class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">Adauga o rezervare

                </button>

            </div>
</form>
            <button id="close">
                Inchide

            </button>
        </div>


    </div>
    </div>

{% endblock %}